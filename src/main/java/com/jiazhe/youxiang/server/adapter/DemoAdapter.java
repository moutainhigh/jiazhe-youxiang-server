/*
 * Copyright (c) 2017 ue-link.com
 * All rights reserved.
 *
 */
package com.jiazhe.youxiang.server.adapter;

import com.jiazhe.youxiang.server.dto.DemoDTO;

/**
 * 在这里编写类的功能描述
 *
 * @author niexiao
 * @created 2018/10/15
 */
public class DemoAdapter {

   /* public static DemoDTO sysRoleReq2DTO(SysRoleReq req) {
        //仅仅是示范，这里就是做一个req到DTO的转换，一般都很简单
        if (req == null) {
            return null;
        }
        DemoDTO demoDTO = new DemoDTO();
        demoDTO.setPageNum(req.getPageNum());
        demoDTO.setPageSize(req.getPageSize());
        demoDTO.setOffset(req.getOffset());
        demoDTO.setLimit(req.getLimit());
        demoDTO.setId(req.getId());
        demoDTO.setName(req.getName());
        demoDTO.setIsSuper(req.getIsSuper());
        demoDTO.setPriority(req.getPriority());
        demoDTO.setPerms(req.getPerms());
        return demoDTO;
    }*/

}